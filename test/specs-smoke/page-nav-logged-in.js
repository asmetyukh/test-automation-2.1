import Login from '../classes/pages/login.page'
import Header from '../classes/elements/header.elem'
import Ratings from '../classes/pages/ratings.page'

describe('Logged-in-user-navigation', () => {

    before(() => {
        Login.openPage()
    })

    it('Page-navigation-logged-in', () => {
        
    })

})